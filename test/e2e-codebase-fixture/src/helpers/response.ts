export function ok<T>(data: T): { success: true; data: T } { return { success: true, data } } export function fail(error: string, code = 400): { success: false; error: string; code: number } { return { success: false, error, code } }
