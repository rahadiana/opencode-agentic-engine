import type { AuthController } from "../controllers/AuthController.js"; export function registerAuthRoutes(ctrl: AuthController, router: { post: (path: string, handler: Function) => void }) { router.post("/auth/login", (req: unknown) => ctrl.login(req as any)); }
