export function isEmail(v: string): boolean { return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v); } export function isPhone(v: string): boolean { return /^\+?[\d\s-]{10,}$/.test(v); } export function notEmpty(v: string): boolean { return v.trim().length > 0 } export function minLength(n: number): (v: string) => boolean { return (v: string) => v.length >= n } export function maxLength(n: number): (v: string) => boolean { return (v: string) => v.length <= n }
