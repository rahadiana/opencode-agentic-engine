export class AuthController { constructor(private auth: import("../services/AuthService.js").AuthService) {} login(req: { body: { email: string; password: string } }): { token: string } { return { token: this.auth.login(req.body.email, req.body.password) } } }
