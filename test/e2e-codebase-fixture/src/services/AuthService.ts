export class AuthService { login(email: string, password: string): string { return `tok-${Date.now()}` } validate(token: string): string | null { return token ? "user-1" : null } logout(token: string): void {} }
