export function validationMiddleware(schema: Record<string, string>) { return (req: { body: Record<string, unknown> }) => { for (const [field, type] of Object.entries(schema)) { if (typeof req.body[field] !== type) throw new Error(`Invalid type for ${field}: expected ${type}`) } } }
