export class RateLimitMiddleware { private hits = new Map<string, number[]>(); check(ip: string): boolean { const now = Date.now(); const window = this.hits.get(ip) ?? []; const recent = window.filter(t => now - t < 60000); recent.push(now); this.hits.set(ip, recent); return recent.length <= 100 } }
